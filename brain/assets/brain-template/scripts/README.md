# Scripts

Keep small scripts and automation here—anything you run from the command line or call from other tools. Use the project venv (e.g. `uv run python scripts/your_script.py`) so dependencies in `pyproject.toml` are available.

- **Agent guidance:** See [AGENTS.md](AGENTS.md) for scope and conventions under this folder.
